{
  "name": "pnpm-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18",
    "pnpm": ">=10"
  }
}
